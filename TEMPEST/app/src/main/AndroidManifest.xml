<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kim.tempest">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartPageActivity"/>
        <activity android:name=".LoginPageActivity"/>
        <activity android:name=".HomePageActivity"/>
        <activity android:name=".NewID0Activity"/>
        <activity android:name=".NewID1Activity"/>
        <activity android:name=".FindIDActivity"/>
        <activity android:name=".HomePageFragment0selectActivity"/>
        <activity android:name=".HomePageFragment1selectActivity"/>
        <activity android:name=".OTPActivity"/>
    </application>

</manifest>